# swagger-ui-dist 5.17.14

Файлы `swagger-ui-bundle.js` и `swagger-ui.css` взяты без изменений из npm-пакета
[swagger-ui-dist](https://www.npmjs.com/package/swagger-ui-dist) версии 5.17.14 (лицензия Apache-2.0,
текст — в файле LICENSE рядом). Лежат локально, чтобы страница `/docs` открывалась без интернета.
