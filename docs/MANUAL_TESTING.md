# Ручная проверка

Подробный чек-лист для проверяющего. Каждый шаг устроен одинаково: что сделать, что должно появиться в AdRobot, что должно оказаться в админке Keitaro. Короткая версия на десять минут есть в [README](../README.md#проверка-своими-руками-за-10-минут).

Обозначения. Офферы названы буквами: А, Б, В, Г, Д. Подставьте любые свои активные офферы. «Keitaro не меняется» значит: обновите страницу кампании в админке и убедитесь, что там всё по-прежнему.

## 0. Подготовка

1. Запустите приложение по [README](../README.md#быстрый-старт) и откройте http://127.0.0.1:8000. Лампа в шапке должна показать «Keitaro: на связи».
2. Откройте в соседнем окне админку Keitaro, раздел «Кампании». После каждого Push обновляйте там страницу кампании, как это делает автор видео.
3. Зайдите в «Настройки» и впишите своё имя в поле «Ваше имя для журнала операций». Оно понадобится в разделе 9.
4. Проверьте, сколько активных офферов видит ключ: на экране «Новая кампания» щёлкните в поле Offer, откроется список. Для полного сценария видео нужно пять офферов. Если их меньше, в разделе 2 есть вариант на три оффера.
5. Все проверочные кампании называйте с общим началом, например `adrobot-check`. Так их проще найти и убрать в разделе 13.

Трекера под рукой нет? Запустите `python scripts/demo_server.py` и откройте http://127.0.0.1:8001. Колонка «В админке Keitaro» в этом режиме не проверяется, всё остальное работает так же.

## 1. Создаватор

### 1.1. Обычное создание и dry-run

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | «Новая кампания». Name: `adrobot-check-1`, Geo: `AU`, в поле Offer найдите оффер А по ID или части названия и выберите его | Под Geo появился значок `AU · Австралия`, под Offer значок с оффером. Справа схема маршрута: Flow 1 ведёт Австралию на `https://google.com`, Flow 2 отдаёт остальных на А со 100 % | Ничего |
| 2 | «Проверить без создания» | Карточка «План: adrobot-check-1» с тремя запросами: `POST /campaigns` и два `POST /streams`. В теле кампании видны домен, группа, источник и параметры источника | Кампании нет |
| 3 | Create | Сообщение о создании. Карточка «Создана: adrobot-check-1»: KT ID, Alias, Гео, номера двух потоков. Если задан адрес трекинг-домена, есть ссылка кампании с кнопкой копирования | Кампания появилась в списке |
| 4 | В Keitaro откройте кампанию и нажмите шестерёнку настроек | | Домен, группа и источник заполнены. На вкладке «Параметры» стоят параметры источника |
| 5 | Посмотрите потоки кампании | | Flow 1: `https://google.com`, фильтр «Страны принимать "Австралия"». Flow 2: офферы, А со 100 % |
| 6 | В AdRobot нажмите «Открыть в редакторе» | Редактор открылся сразу с двумя потоками, Fetch не понадобился. Лампа потока Flow 2: «совпадает с Keitaro» | |

### 1.2. Несколько стран и несколько офферов

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | Name: `adrobot-check-2`, Geo: `mx, au; Румыния`, офферы А, Б и В | Три значка стран: MX, AU, RO. На схеме доли 33 / 33 / 34, лишний процент у В | |
| 2 | Create | Кампания создана | В фильтре Flow 1 три страны. В Flow 2 офферы 33 / 33 / 34, у В стоит 34 |

### 1.3. Отдельная кампания на страну

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | Name: `adrobot-check-3`, Geo: `MX, AU`, оффер А. Раскройте «Домен, группа, источник и другое» и отметьте «Отдельная кампания на каждую страну» | Схема сообщает, что будет создано две кампании | |
| 2 | Create | Две карточки: `adrobot-check-3 [MX]` и `adrobot-check-3 [AU]` | Две кампании. У каждой в Flow 1 своя страна |
| 3 | Повторите с именем `adrobot-check-{geo}-x` | Названия `adrobot-check-MX-x` и `adrobot-check-AU-x` | Две кампании с такими названиями |

### 1.4. Ошибки ввода

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | Geo: `ZZ`, остальное заполнено, Create | Красный значок `ZZ ?`. Под формой: «Не распознано: ZZ.» Запрос не отправлен | Ничего |
| 2 | Geo: `UK` | Значок `GB · Великобритания`. После Create кампания создаётся | В фильтре Flow 1 стоит Великобритания. Сам Keitaro сохранил бы `UK` без ошибки, и фильтр никогда бы не сработал |
| 3 | Оставьте пустым Name, затем не выбирайте оффер | «Введите название кампании.», «Выберите хотя бы один оффер из списка.» | Ничего |
| 4 | В поле «Куда редиректить страну из Flow 1» впишите `google.com`, Create | «Адрес редиректа должен быть полным URL вида https://google.com.» | Ничего |
| 5 | Создайте кампанию с названием, которое уже есть в трекере | Сообщение: кампания с таким названием уже есть, указан её номер. Второй клик по Create создаёт дубль осознанно | После первого клика ничего. После второго появляется вторая кампания с тем же названием |
| 6 | В поле Alias впишите alias уже существующей кампании | Карточка «Не создана»: alias уже занят. Подмены на случайный alias нет | Ничего |
| 7 | Быстро щёлкните Create дважды | Кнопка блокируется на время запроса. Кампания одна | Одна кампания |

Несуществующий оффер в форме выбрать нельзя: поле принимает только варианты из списка. Проверьте отказ через API, в Swagger (`/docs`) или командой:

```bash
curl -s -X POST http://127.0.0.1:8000/api/campaigns \
  -H "Content-Type: application/json" \
  -d '{"name": "adrobot-check-bad", "geo": "AU", "offer_id": 999999999}'
```

Ожидается ответ 422 с кодом `validation`. В тексте сказано: «Оффер #999999999 не найден в Keitaro (или недоступен ключу API).» Кампания в трекере не появляется.

Идемпотентность удобно увидеть так же. Подставьте вместо `101` номер своего оффера и выполните команду дважды:

```bash
curl -s -X POST http://127.0.0.1:8000/api/campaigns \
  -H "Content-Type: application/json" -H "Idempotency-Key: adrobot-check-idem-1" \
  -d '{"name": "adrobot-check-idem", "geo": "AU", "offer_id": 101}'
```

Второй ответ повторяет первый и содержит `"replayed": true`. В Keitaro кампания одна. Тот же ключ с другим телом даёт 409 `idempotency_mismatch`.

## 2. Редактор: сценарий видео

Нужны пять офферов. Создайте кампанию `adrobot-check-video` с гео `AU` и тремя офферами: А, Б, В, именно в этом порядке. Получится стартовая картина из ролика: 33 / 33 / 34. Откройте её в редакторе. Таймкоды указаны по видео-ТЗ, разбор лежит в [VIDEO_LOGIC.md](VIDEO_LOGIC.md).

| № | Таймкод | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|---|
| 1 | 0:41 | Нажмите Fetch streams from KT | Flow 2 показан карточкой с офферами: В 34, А 33, Б 33. Строки идут по убыванию доли. Flow 1 показан одной строкой без офферов. Лампа: «совпадает с Keitaro» | А 33, Б 33, В 34 |
| 2 | 2:04 | В строке добавления найдите Г, выберите, нажмите Add | Четыре оффера по 25. У Г метка «новый». Поток жёлтый, лампа «не опубликован». В жёлтой плашке список изменений: у А, Б и В доля меняется на 25, Г появится с долей 25. Появились Push to KT и Cancel | Keitaro не меняется: 33 / 33 / 34 |
| 3 | 2:30 | Push to KT | Сообщение о публикации. Поток белый, плашки нет | А 25, Б 25, В 25, Г 25. Г стоит последним |
| 4 | 2:57 | Добавьте Д через Add | Пять офферов по 20, поток жёлтый | Keitaro не меняется: четыре по 25 |
| 5 | 3:07 | Cancel | Строка Д исчезла совсем. Снова четыре по 25, поток белый. Д по-прежнему находится в поиске офферов | Keitaro не меняется |
| 6 | 3:26 | Remove у оффера Б | Б серый, 0 %, метка `removed`, вместо Remove стоит Bring back. Остальные: Г 34, А 33, В 33. Лишний процент у Г, он добавлен последним. Поток жёлтый | Keitaro не меняется: четыре по 25 |
| 7 | 3:41 | Push to KT | Поток белый. Б остался серой строкой в архиве | А 33, В 33, Г 34. Оффера Б нет |
| 8 | 3:59 | Bring back у Б | Четыре по 25. Б вернулся на прежнее место в таблице, метка «вернётся». Поток жёлтый | Keitaro не меняется |
| 9 | 4:03 | Push to KT | Поток белый | А 25, В 25, Г 25, Б 25. Б стоит последним |
| 10 | 4:15 | В Keitaro щёлкните по названию Flow 2, откройте вкладку «Схема», удалите крестиком А и В. «Уравнять» не нажимайте. «Применить», затем «Сохранить» | | Г 25, Б 25. Keitaro сам пишет «В сумме 50%» |
| 11 | 4:34 | В AdRobot нажмите Fetch streams from KT | Сообщение: получено два потока, два оффера ушли в архив. Активные: Б 25, Г 25. Серые: А 0 %, В 0 %, у обоих Bring back. Доли не пересчитаны. Черновика нет: нет ни плашки, ни Push. Сверх оригинала появилось предупреждение «Сумма долей 50%, а должна быть 100%.» с кнопками «Пересчитать» и «Выровнять поровну». Не нажимайте их | Keitaro не меняется |
| 12 | 5:09 | Щёлкните по значку pin рядом с долей Б | Значок стал тёмным. Цифры те же. Поток не пожелтел | Keitaro не меняется |
| 13 | 5:27 | Bring back у А | А 38 с меткой «вернётся», Г 37, Б 25 с тёмным значком. В остался серым. Предупреждение о сумме исчезло. Поток жёлтый | Keitaro не меняется |
| 14 | 5:44 | Push to KT | Поток белый | Г 37, Б 25, А 38. Сумма 100 |

Финал совпадает с последним кадром ролика: в обёртке 38 / 37 / 25 с закреплением, в Keitaro 37 / 25 / 38.

### Вариант на три оффера

Создайте кампанию `adrobot-check-mini` с гео `AU` и одним оффером А.

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | Add Б | 50 / 50, поток жёлтый | А 100 |
| 2 | Push to KT | Поток белый | А 50, Б 50 |
| 3 | Add В, затем Cancel | Сначала 33 / 33 / 34 с лишним процентом у В. После Cancel снова 50 / 50, строки В нет | Keitaro не меняется |
| 4 | Add В, Push to KT | Поток белый | А 33, Б 33, В 34 |
| 5 | Remove Б, Push to KT | А 50, В 50. Б серый, 0 % | А 50, В 50 |
| 6 | Bring back Б, Push to KT | А 33, В 33, Б 34. Лишний процент у возвращённого | А 33, В 33, Б 34 |
| 7 | В Keitaro удалите А из Flow 2, «Уравнять» не нажимайте, сохраните | | В 33, Б 34 |
| 8 | Fetch streams from KT | А ушёл в архив. В 33, Б 34 без пересчёта. Предупреждение о сумме 67 % | |
| 9 | Закрепите Б, затем Bring back у А | Б 34 с закреплением, В 33, А 33. Поток жёлтый | Keitaro не меняется |
| 10 | Push to KT | Поток белый | В 33, Б 34, А 33. Сумма 100 |

Этот же сценарий автоматически проходит `python scripts/live_e2e.py`.

## 3. Pin, ручная доля, «Пересчитать», «Выровнять поровну»

Возьмите поток с тремя офферами 33 / 33 / 34 (А, Б, В). В Keitaro на этих шагах ничего не меняется до Push.

| № | Сделайте | В AdRobot |
|---|---|---|
| 1 | Щёлкните по проценту у В, введите `50`, нажмите Enter | В 50 с тёмным значком pin. А 25, Б 25. Рядом с новой долей зачёркнута прежняя. Поток жёлтый |
| 2 | Снова щёлкните по проценту у В, введите `99`, Enter | Отказ: двум незакреплённым офферам остаётся 1 %, а каждому нужен хотя бы 1 %. Доли не изменились |
| 3 | Введите `0`, затем `101` | Отказ проверки запроса: доля принимается только от 1 до 100. Пояснение валидатора в этом сообщении приходит на английском |
| 4 | Щёлкните по проценту, измените число и нажмите Esc | Ввод отменён, значение прежнее |
| 5 | Закрепите А и Б значками pin | Цифры не меняются |
| 6 | Remove у В | А 25 и Б 25 остались: оба закреплены, делить остаток некому. Лампа «нужна правка». Сообщение: сумма долей 50 %, все офферы закреплены. Push выключен |
| 7 | «Пересчитать» | Ничего не изменилось: свободных офферов нет |
| 8 | «Выровнять поровну» | А 50, Б 50, закрепления сняты. Push доступен |
| 9 | Cancel | Вернулись 33 / 33 / 34 и оффер В. Закрепления кнопка Cancel не восстанавливает: pin не входит в черновик |
| 10 | Закрепите любую долю, затем щёлкните по тёмному значку pin ещё раз | Значок снова светлый, доля откреплена. Цифры не менялись |

## 4. Конфликт с правкой в Keitaro

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | В AdRobot сделайте черновик: добавьте оффер или задайте долю вручную | Поток жёлтый | Keitaro не меняется |
| 2 | В Keitaro откройте поток, на вкладке «Схема» измените долю или удалите оффер. «Применить», «Сохранить» | | Поток изменён |
| 3 | В AdRobot нажмите Push to KT | Диалог «Поток изменили прямо в Keitaro». В нём два списка: «Сейчас в Keitaro» и «AdRobot ожидал увидеть». Кнопки: «Отмена», «Fetch: подтянуть из Keitaro», «Опубликовать поверх» | Правка из шага 2 цела |
| 4 | Выберите «Fetch: подтянуть из Keitaro» | Состояние трекера подтянуто, ваш черновик сохранён, поток жёлтый. Если сумма разошлась, появилось предупреждение: нажмите «Пересчитать» | Keitaro не меняется |
| 5 | Push to KT | Конфликта больше нет, поток белый | В потоке ваш черновик |
| 6 | Повторите шаги 1–3 и выберите «Опубликовать поверх» | Поток белый. В журнале Push отмечен как принудительный | Чужая правка перезаписана вашим черновиком |

## 5. Fetch при черновике

| № | Сделайте | В AdRobot |
|---|---|---|
| 1 | Сделайте черновик и нажмите Fetch streams from KT | Диалог «В кампании есть неопубликованные изменения». Кнопки: «Отмена», «Сбросить черновик», «Сохранить черновик» |
| 2 | «Отмена» | Ничего не произошло |
| 3 | Снова Fetch, «Сохранить черновик» | Черновик на месте, поток жёлтый |
| 4 | Снова Fetch, «Сбросить черновик» | Поток белый и повторяет Keitaro. Добавленные и неопубликованные офферы исчезли |

## 6. История и откат

| № | Сделайте | В AdRobot | В админке Keitaro |
|---|---|---|---|
| 1 | После нескольких Push нажмите «История» в шапке потока | Диалог со снимками «Перед публикацией» и датой. В каждом снимке перечислены офферы и доли, какими они были в Keitaro до этой публикации | |
| 2 | У нужного снимка нажмите «Вернуть в черновик» | Сообщение: снимок загружен в черновик. Поток жёлтый, доли как в снимке. Офферы, которых в снимке не было, ушли в архив. Закрепления сняты | Keitaro не меняется |
| 3 | Push to KT | Поток белый | Состояние из снимка |

## 7. Пустой поток

| № | Сделайте | В AdRobot |
|---|---|---|
| 1 | Уберите кнопкой Remove все офферы потока | Лампа «нужна правка». Сообщение «В потоке нет активных офферов.» Кнопка Push выключена. В Keitaro при этом ничего не меняется |
| 2 | «Пересчитать» или «Выровнять поровну» | Отказ с тем же текстом |
| 3 | Cancel либо Bring back | Поток снова рабочий |

Опубликовать пустой поток из интерфейса нельзя, это сделано намеренно строго. Через API можно: `POST /api/streams/{id}/push` с пустым телом ответит 422 `empty_stream`, с телом `{"allow_empty": true}` опубликует. Номер потока AdRobot виден в адресах запросов на вкладке «Сеть» браузера и в ответе `GET /api/campaigns/{id}`.

## 8. Оффер, недоступный ключу

Проверка возможна, если в трекере есть кампания, в потоке которой стоит оффер, не видимый вашему ключу. Например, его завёл другой пользователь.

| № | Сделайте | В AdRobot |
|---|---|---|
| 1 | На экране «Кампании» впишите номер такой кампании в поле KT ID и нажмите «Открыть по ID» | Вместо названия стоит «Оффер #N» с красной меткой «нет в справочнике». Доля взята из Keitaro |
| 2 | Сделайте любую правку с другими офферами и нажмите Push to KT | Публикация проходит. Незнакомый оффер остаётся в потоке со своей долей |
| 3 | Уберите такой оффер через Remove, опубликуйте и попробуйте Bring back | Отказ: оффер не найден в Keitaro или недоступен ключу |

## 9. Журнал

1. Откройте «Журнал». Каждый шаг из разделов выше записан отдельной строкой: «Создание кампании», «Проверка без создания», «Fetch streams», «Add», «Remove», «Bring back», «Pin», «Unpin», «Доля вручную», «Пересчёт долей», «Выравнивание долей», «Push to KT», «Cancel», «Откат к снимку».
2. У неудачных операций красная метка «ошибка», в колонке «Что произошло» стоит текст ошибки. Пример: отказ на долю 99 из раздела 3.
3. В колонке «Кто» стоит имя, заданное в подготовке. Журнал самого Keitaro видит только API-ключ.
4. В колонке «мс» стоит длительность операции вместе с запросами к трекеру.
5. Фильтр «Только ошибки» оставляет только красные строки. Кнопки внизу листают журнал по 50 записей.

## 10. Настройки

| № | Сделайте | В AdRobot |
|---|---|---|
| 1 | Выберите группу и источник по умолчанию, нажмите «Сохранить» | Сообщение о сохранении. После перезагрузки страницы под полем написано «задано здесь» |
| 2 | Откройте «Новая кампания» и раскройте «Домен, группа, источник и другое» | В списках первым стоит вариант «По умолчанию» с выбранным номером |
| 3 | Верните в настройках «Автоматически» и сохраните | Под полем снова «определено автоматически» либо «из файла .env» |
| 4 | Посмотрите на поле домена | Если ключу виден список доменов, это выпадающий список. Если нет, это поле для номера с подсказкой, откуда взят текущий домен |
| 5 | Впишите адрес трекинг-домена, например `https://go.example.com`, сохраните | В редакторе и в карточке созданной кампании появилась ссылка вида `https://go.example.com/<alias>`. На потоки это не влияет |
| 6 | «Проверить ещё раз» в блоке «Связь с Keitaro» | Статус связи обновился |

## 11. Тёмная тема, мобильная ширина, клавиатура

1. «Настройки», поле «Тема»: выберите «Тёмная». Интерфейс перекрасился. Выбор сохраняется после перезагрузки. Вариант «Как в системе» следует за настройкой ОС.
2. Пройдите по экранам в тёмной теме: жёлтый поток, серые строки архива, шкала долей и диалоги читаются.
3. Сузьте окно до ширины телефона, меньше 720 px. Меню ушло на вторую строку. В таблице офферов скрылись колонки Stats и Trends. Таблицы прокручиваются по горизонтали. Форма создания стала одной колонкой.
4. Клавиатура. Первое нажатие Tab показывает ссылку «К содержимому». В поиске офферов работают стрелки, Enter и Esc. Диалог закрывается по Esc, фокус не уходит за его пределы и возвращается на кнопку, которая его открыла.

## 12. Токен доступа и сбои

### Токен

| № | Сделайте | Ожидается |
|---|---|---|
| 1 | Остановите приложение. Впишите в `.env` строку `ADROBOT_AUTH_TOKEN=` с длинным случайным значением. Запустите снова и обновите страницу | Диалог «Нужен токен доступа» |
| 2 | Введите неверный токен | Диалог появляется снова |
| 3 | Введите верный токен | Интерфейс работает. Токен хранится до закрытия вкладки, новая вкладка спросит его заново |
| 4 | `curl -i http://127.0.0.1:8000/api/health` | Ответ 401 и заголовок `WWW-Authenticate: Bearer` |
| 5 | Та же команда с `-H "Authorization: Bearer <токен>"` | Ответ 200 |

### Сбои

| № | Сделайте | Ожидается |
|---|---|---|
| 1 | Испортите `KEITARO_API_KEY` и перезапустите | Лампа красная: «Keitaro: нет связи». В настройках написано: Keitaro отклонил ключ API (401). Список кампаний и редактор открываются из базы. Fetch, Push и «Обновить из Keitaro» показывают ту же причину. Локальные данные целы |
| 2 | Укажите в `KEITARO_BASE_URL` адрес, где никто не отвечает | Через несколько секунд повторов: «Нет связи с Keitaro» с методом и путём запроса |
| 3 | Укажите адрес сайта, который не является трекером | Сообщение о том, что пришёл не JSON, с советом проверить `KEITARO_BASE_URL`. На некоторых сайтах вместо него придёт «не найдено» |
| 4 | Удалите из `.env` адрес и ключ | Лампа жёлтая: «Keitaro: не настроен». Вместо экранов показана инструкция «Осталось подключить Keitaro». «Настройки» открываются |
| 5 | При открытой странице остановите сервер AdRobot и нажмите любую кнопку | Сообщение: нет связи с сервером AdRobot |
| 6 | Переименуйте файл базы и запустите приложение без `alembic upgrade head` | Ответ 503 с подсказкой выполнить `alembic upgrade head` |
| 7 | Сделайте черновик, отключите сеть, нажмите Push to KT, включите сеть, нажмите Push снова | Первый Push кончается ошибкой, поток остаётся жёлтым, черновик цел. Второй Push проходит |

После проверок верните в `.env` рабочие значения.

## 13. Как убрать за собой

1. Откройте проверочную кампанию в редакторе. Внизу страницы нажмите «Кампанию в архив» и подтвердите.
2. В AdRobot кампания пропадает из списка. В Keitaro она переносится в архив. Оттуда её можно восстановить, по документации архив хранится ограниченное время.
3. Повторите для каждой кампании с началом `adrobot-check`. Не забудьте пары из раздела 1.3 и дубль из раздела 1.4.
4. Кампании, созданные командами `curl`, тоже есть в списке AdRobot: они прошли через тот же API. Уберите их так же.
5. Скрипт `scripts/live_e2e.py` убирает свою кампанию сам, если не указан флаг `--keep`.

База AdRobot после проверок хранит журнал и скрытые кампании. Чтобы начать с чистого листа, остановите приложение, удалите файлы `data/adrobot.sqlite3*` и выполните `alembic upgrade head`.
